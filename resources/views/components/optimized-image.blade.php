<img 
    src="{{ $src }}"
    alt="{{ $alt }}" 
    {{ $width ? "width={$width}" : '' }}
    {{ $height ? "height={$height}" : '' }}
    loading="{{ $loading }}"
    {{ $class ? "class={$class}" : '' }}
    {{ $attributes }}
>
