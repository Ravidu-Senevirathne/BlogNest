<img 
    srcset="{{ $srcset }}"
    sizes="{{ $sizes }}"
    src="{{ $defaultUrl }}"
    alt="{{ $alt }}"
    loading="{{ $loading }}"
    {{ $class ? "class={$class}" : '' }}
    {{ $attributes }}
>
